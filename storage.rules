rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow public reads for generated download URLs (tokenized) and previews.
    match /templates/{templateId}/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}

